# Only needed to re-record the qualitative extraction fixtures.
# Scoring, the validation harness and the whole test suite run offline
# against the recorded fixtures in data/fixtures/extractions/.
OPENAI_API_KEY=sk-...

# Optional: point at an OpenAI-compatible gateway instead of api.openai.com.
# OPENAI_BASE_URL=https://...

# Optional: override the extraction model.
# EXTRACTION_MODEL=gpt-5.2-2025-12-11
